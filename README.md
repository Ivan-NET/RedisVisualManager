# RedisVisualManager
Redis Visual Manager
An additional tool For RedisDesktopManager which not good at plenty of data.
While Redis includes plenty of data, it will lead to timeout, so use search key pattern to filter keys first.
